<%= form_with(model: grupoingrediente) do |form| %>
  <% if grupoingrediente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grupoingrediente.errors.count, "error") %> prohibited this grupoingrediente from being saved:</h2>

      <ul>
        <% grupoingrediente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
