<div class="row">
  <input id="buscar" type="text" class="form-control"
   placeholder="Buscar..." />
  <table id="addPlatosAdmin" class="mt-2">
    <thead>
      <tr>
        <th colspan="3">Nombre del plato</th>
      </tr>
    </thead>
    <tbody>
      <% if current_cliente.suscriptor == 1 %>
        <% @platos_m.each do |p| %>
          <tr>
            <td>
              <%= p.nombre %>
            </td>
            <td>
              <%= p.cliente.nombre %>
            </td>
            <td>
              <%= button_to "Añadir",
              menu_platos_path(:plato_id => p.id,
              :menu_id => @menu.id),
              class: "btn btn-success btn-sm" %>
            </td>
          </tr>
        <% end %>
      <% else %>
        <% @platos_m_s.each do |p| %>
          <tr>
            <td colspan="2">
              <%= p.nombre %>
            </td>
            <td>
              <%= button_to "Añadir",
              menu_platos_path(:plato_id => p.id,
              :menu_id => @menu.id),
              class: "btn btn-success btn-sm" %>
            </td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
<%= javascript_pack_tag "buscarAddPlatosAdmin" %>
