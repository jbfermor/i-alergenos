<div class="row">
  <input id="buscar" type="text" class="form-control"
   placeholder="Buscar..." />
  <table id="addIngredientes" class="mt-2">
      <thead>
        <tr>
          <th colspan="2">Nombre</th>
        </tr>
      </thead>
      <tbody>
        <% Ingrediente.all.each do |i| %>
        <tr>
          <td>
            <%= i.nombre %>
          </td>
          <td>
            <%= button_to "Añadir",
            plato_ingredientes_path(:ingrediente_id => i.id,
            :plato_id => @plato.id),
            class: "btn btn-success btn-sm" %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
    <div class="d-flex justify-content-center">
      <%== pagy_bootstrap_nav(@pagy_ingredientes) if @pagy_ingredientes.pages > 1 %>
    </div>
</div>
<%= javascript_pack_tag "buscarAddIngredientes" %>
